apiVersion: batch/v1
kind: Job
metadata:
  name: scraper-purchase-history-job
spec:
  template:
    metadata:
      labels:
        app: scraper-purchase-history-job
    spec:
      restartPolicy: Never
      containers:
      - args:
        - bash
        - -c
        - cd /root/scraper/; java -jar web-scraper-server-*.jar --batch=purchase_history --spring.config.location=file:application.yaml
        env:
        - name: SCRAPER_DB_PORT_3306_TCP_ADDR
          value: scraper-db
        image: tc3jp/scraper-app:dev1.0
        name: scraper-purchase-history-job
